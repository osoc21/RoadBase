<AuButtonGroup class="views">
    <AuButton @skin="primary" {{on 'click' (fn @onClick true)}} >
      Straat
    </AuButton>
    <AuButton @skin="primary" {{on 'click' (fn @onClick false)}} >
      Satelliet 
    </AuButton>
</AuButtonGroup>
